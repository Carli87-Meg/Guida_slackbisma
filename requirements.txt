# pip install -r requirements.txt
faster-whisper>=1.0
pillow>=10
reportlab>=4
opencv-python-headless>=4.9   # facoltativo: stima di nitidezza dei frame
# ffmpeg deve essere installato a parte e presente nel PATH
